export const typeDoc = Object.freeze({
  DNI: 'DNI',
  CE: 'CE'
})

export const gender = Object.freeze({
  M: 'MASCULINO',
  F: 'FEMENINO'
})

export const role = Object.freeze({
  USUARIO: 'USER',
  ADMIN: 'ADMIN'
})

export const typeSave = Object.freeze({
  FAVORITO: 'FAVORITO',
  VER_MAS_TARDE: 'VER_MAS_TARDE'
})
